<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>家系図アプリ - Kakaizu Neo</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gradient-to-br from-slate-50 to-blue-50 min-h-screen">
    <nav class="bg-white shadow-lg border-b border-slate-200">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <div class="flex items-center">
            <span class="text-3xl mr-3">🌳</span>
            <h1 class="text-2xl font-bold bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text text-transparent">
              家系図アプリ
            </h1>
          </div>
          <div class="flex items-center space-x-4">
            <%= link_to "家系図", family_tree_path, class: "px-4 py-2 rounded-lg font-medium transition-all hover:bg-blue-50 text-slate-700 hover:text-blue-600" %>
            <%= link_to "人物一覧", people_path, class: "px-4 py-2 rounded-lg font-medium transition-all hover:bg-blue-50 text-slate-700 hover:text-blue-600" %>
            <%= link_to "関係一覧", relationships_path, class: "px-4 py-2 rounded-lg font-medium transition-all hover:bg-blue-50 text-slate-700 hover:text-blue-600" %>
          </div>
        </div>
      </div>
    </nav>

    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
      <% if notice %>
        <div class="mb-6 bg-green-50 border-l-4 border-green-500 p-4 rounded-r-lg shadow-sm">
          <p class="text-green-800 font-medium"><%= notice %></p>
        </div>
      <% end %>
      
      <% if alert %>
        <div class="mb-6 bg-red-50 border-l-4 border-red-500 p-4 rounded-r-lg shadow-sm">
          <p class="text-red-800 font-medium"><%= alert %></p>
        </div>
      <% end %>

      <%= yield %>
    </main>

    <footer class="mt-16 bg-white border-t border-slate-200 py-6">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center text-slate-600">
        <p>© 2024 Kakaizu Neo - 家系図管理システム</p>
      </div>
    </footer>
  </body>
</html>
